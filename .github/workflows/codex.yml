name: OpenAI Codex

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]

jobs:
  codex:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@codex')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@codex')) ||
      (github.event_name == 'issues' && contains(github.event.issue.body, '@codex'))
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install OpenAI Codex CLI
        run: npm install -g @openai/codex

      - name: Run OpenAI Codex (non-interactive, API key auth)
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN:   ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail

          # Lokální git identita (NE --global)
          git config user.name  "OpenAI Codex"
          git config user.email "codex@openai.com"

          # Vytáhni text zadání podle typu eventu
          if [[ "${{ github.event_name }}" == "issue_comment" ]]; then
            PROMPT="${{ github.event.comment.body }}"
          elif [[ "${{ github.event_name }}" == "pull_request_review_comment" ]]; then
            PROMPT="${{ github.event.comment.body }}"
          else
            PROMPT="${{ github.event.issue.body }}"
          fi

          # Odstraň '@codex' (case-insensitive) a ořež bílé znaky
          CLEAN_PROMPT="$(printf '%s' "$PROMPT" | sed 's/@codex//Ig' | sed 's/^[[:space:]]*//; s/[[:space:]]*$//')"
          if [[ -z "$CLEAN_PROMPT" ]]; then
            CLEAN_PROMPT="Proveď požadavek popsaný v souvisejícím issue/komentáři co nejbezpečněji; pokud je specifikace vágní, navrhni konkrétní TODO a diffy."
          fi

          echo "🤖 OpenAI Codex processing:"
          echo "-----"
          printf '%s\n' "$CLEAN_PROMPT"
          echo "-----"

          # DŮLEŽITÉ: vynutíme auth metodou 'apikey', jinak se CLI pokusí o ChatGPT login a v CI spadne na 401
          # Neinteraktivní běh přes 'exec'
          codex --config preferred_auth_method="apikey" exec "$CLEAN_PROMPT" | tee co_
